apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: daprConfig
spec:
  tracing:
    samplingRate: "1"
  # Enable actor reentrancy for better concurrency
  features:
    - name: Actor.Reentrancy
      enabled: true
